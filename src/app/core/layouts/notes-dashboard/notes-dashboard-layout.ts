import {Component} from '@angular/core';
import {RouterOutlet} from '@angular/router';

@Component({
    selector: 'app-notes-dashboard-layout',
    templateUrl: './notes-dashboard-layout.html',
    imports: [
        RouterOutlet
    ]
})

export default class NotesDashboardLayout {
}
